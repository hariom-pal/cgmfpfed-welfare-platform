<span class="badge rounded-pill {{ $active ? 'text-bg-success' : 'text-bg-secondary' }}">
    <i class="fa-solid {{ $active ? 'fa-check' : 'fa-minus' }} me-1"></i>{{ $active ? 'Active' : 'Inactive' }}
</span>
